{
  "name": "orb",
  "version": "0.1.0",
  "description": "RAG Chatbot for Obsidian Vaults",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:staged": "prettier --write --staged",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
